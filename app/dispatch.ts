/**
 * Routes a ParsedCommand to a builtin or an external executable.
 *
 * TODO (refactor step 5): Replace the switch in main.ts after all builtin
 * handlers are registered. This module should not parse raw input or prompt.
 */
