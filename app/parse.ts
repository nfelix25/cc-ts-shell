/**
 * Converts one raw input line into a ParsedCommand.
 *
 * This should be a pure module: no readline, process, filesystem, logging, or
 * command execution. Move the parser before trying to simplify its algorithm,
 * and protect its current behavior with tests first.
 *
 * TODO (refactor step 2): Move parseInput here and export it.
 */
