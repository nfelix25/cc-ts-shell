/**
 * Finds commands on PATH and runs external processes.
 *
 * TODO (refactor step 3): Extract lookup and execution as separate functions;
 * the type builtin needs lookup without launching the command.
 */
