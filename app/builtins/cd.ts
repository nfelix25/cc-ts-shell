/**
 * Implements only the cd builtin.
 *
 * TODO (refactor step 4): Move cd and tilde-expansion behavior here. Preserve
 * current behavior during the move; improve error handling in a later change.
 */
